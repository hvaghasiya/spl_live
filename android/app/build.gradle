plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {
    namespace = "com.example.spllive"
    compileSdk = 35
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }
    flavorDimensions "abi"
//    splits {
//        abi {
//            enable true
//            reset() // Clear default 'all' ABIs
//            include 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64','universalApk' // Specify desired ABIs
//            universalApk true
//        }
//    }
    splits {
        abi {
            enable true
            universalApk true // This line creates the universal APK
        }
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.spllive"
        minSdk = 23
        targetSdk = 35
        versionCode = 1001
        versionName = flutter.versionName
    }
//    productFlavors {
//
//        arm {
//            dimension "abi"
//            versionCode 1100 // Version code for 'armeabi-v7a' split
//
//        }
//
//        arm64 {
//            dimension "abi"
//            versionCode 1110 // Version code for 'arm64-v8a' split
//
//        }
//
//        x86 {
//            dimension "abi"
//            versionCode 1120 // Version code for 'x86' split
//
//        }
//
//        x86_64 {
//            dimension "abi"
//            versionCode 1130 // Version code for 'x86_64' split
//
//        }
//
//    }
    signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile file(keystoreProperties['storeFile'])
            storePassword keystoreProperties['storePassword']
        }
    }
    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.release
//            minifyEnabled true
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}

